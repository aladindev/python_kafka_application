import subprocess
import os

#subprocess.run(["python3", "apps/upbit/scheduler/api_request_scheduler.py"])
#subprocess.run(["python3", "apps/upbit/scheduler/routers/api_request_upbit.py"])

#main 실행이 아닌 스케줄러 직접 실행으로 변경

